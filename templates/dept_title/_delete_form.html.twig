<form method="post" action="{{ path('app_dept_title_delete', {'id': dept_title.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dept_title.id) }}">
    <button class="btn">Delete</button>
</form>
