<form method="post" action="{{ path('app_demand_delete_all') }}" onsubmit="return confirm('Are you sure you want to delete all treated items?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_all') }}">
    <button class="btn btn-danger" type="submit"><i class="fa fa-trash"></i></button>
</form>
